# Moon Rover API

# Requirements
  1. Spring Boot
  2. Spring Web
  3. Maven

# install & test
   mvn install
   mvn test
note: project can also be imported to favourite Java supported IDE like intellij or eclipse,etc

# Install using docker
  docker build -t moonroverapiserver
  docker run -p 80:8080 moonroverapiserver